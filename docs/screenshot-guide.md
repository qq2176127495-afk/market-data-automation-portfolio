# Screenshot Guide

Recommended screenshots:

1. Project structure with `docs/` and `examples/`.
2. Sanitized Markdown report summary.
3. Synthetic CSV rows or field schema.

Do not include:

- API keys
- `.env` files
- VPS IPs
- account balances
- order IDs
- raw production logs
- exact private trading parameters
